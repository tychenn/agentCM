The previous response failed validation. Produce a complete corrected response
for the same task and output format, not a patch or an explanation. Recheck the
original input; do not treat the previous response as evidence.
PREVIOUS RESPONSE (JSON-encoded string)
{previous_response_json}
VALIDATION ERRORS (JSON array of strings)
{validation_errors_json}
